/* Name: BUYONDO EMMANUEL */
/* Student Number: 24/U/BIO/04517/PD */

#include <stdio.h>

int main(void)
{
    float amountTosend, transactionFee(%%), feeAmount, totalAmount;
    // Prompt the user for the amount to send and the transaction fee percentage
    
    
    printf("Enter amountToSend:");
    scanf("%f", &amountTosend);
    printf("Enter transactionFee(%%):");
    scanf("%f", &transactionFee(%%));
   
    // Calculate the transaction fee and the total amount to be paid
    
    feeAmount = (amountTosend * transactionFee(%%))/ 100;
    totalAmount = amountTosend + feeAmount;
   
    // Display the total amount to be paid
   
    printf("feeAmount%.2f\n", feeAmount);
    printf("totalAmount%.2f\n", totalAmount);

 return 0;

}


